"use strict";

const category = 'toys';

// console.log('https://someurl.com/' + category + '/' + '4'); // конкатенация строк (склеивание строк)
console.log(`https://someurl.com/${category}/4`); // интерполяция (переменная внутри строки) ``

const user = "Ivan";

alert(`Привет, ${user}`);